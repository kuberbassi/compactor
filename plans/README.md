# Compactor implementation plans

Generated by the `improve` skill on 2026-09-14 and completed on 2026-09-15.
The plans were reviewed in an isolated snapshot before their patch series was
applied to `main` on top of baseline commit `de7b51b`.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---:|---:|---|---|
| 001 | Fix object-URL ownership and failure cleanup | P1 | S | - | DONE - merged as `86b77d0`, `e9b6bdb` |
| 002 | Enforce one strict quality gate | P1 | S | - | DONE - merged as `2b41441` |
| 003 | Characterize critical editor workflows | P1 | M | 001, 002 | DONE - merged as `d5e5f37` |
| 004 | Centralize the simple PDF result lifecycle | P1 | M | 003 | DONE - merged as `c5cb365`, `f644e7d` |
| 005 | Make the processed-file metric honest | P2 | M | 002 | DONE - merged as `eee3c38` |

Status values: TODO, IN PROGRESS, DONE, BLOCKED, REJECTED.

## Dependency notes

- 003 follows 001 so its object-URL assertions encode corrected ownership,
  rather than preserving the current retry bug.
- 004 follows 003 because the large PDF orchestrator currently lacks enough
  component-level behavior coverage for a safe structural rewrite.
- 005 is independent of the UI refactor but should inherit the quality gate
  and API test command established by 002.

## Reviewed and merged implementation

- Snapshot baseline: `599fbaa`
- Reviewed snapshot head: `ad35b42`
- Integrated `main` head before this index refresh: `eee3c38`
- The patch series was applied after committing the pre-existing working-tree
  baseline as `de7b51b`; no source changes were discarded.

## Verified baseline

- `npm.cmd run lint`: exit 0.
- `npm.cmd run test`: 45 files and 165 tests passed.
- `npm.cmd run build`: exit 0.
- `node_modules\.bin\tsc.cmd -p tsconfig.app.json --strict --pretty false`:
  exit 0.
- `npm.cmd audit --omit=dev --audit-level=high`: zero vulnerabilities.

## Findings queued for a later planning pass

- Split route-owned CSS out of the 6,500-line global stylesheet and establish
  one owner for shared workbench geometry. This needs visual baselines first.
- Dynamically import conversion engines inside the selected conversion branch;
  the current Universal Converter route chunk is approximately 1.29 MB.
- Add focused browser coverage for real Canvas, Blob, worker, and download
  behavior after the component characterization foundation in plan 003.
- Calibrate operation-specific browser memory limits for large audio, image,
  video, and aggregate queue inputs.
- Build a complete production CSP only after intentional embedding, worker,
  analytics, blob-media, and font requirements are documented.

## Findings considered and rejected

- Immediate dependency upgrades: not worth coupling to this cleanup because
  the production audit reports zero actionable vulnerabilities.
- A universal framework for every media tool: rejected because it would hide
  domain-specific progress, partial-success, and cancellation behavior behind
  a premature abstraction.
